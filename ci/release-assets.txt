# SPDX-License-Identifier: LGPL-2.1-or-later
#
# What a tag of this repository publishes as release assets. One glob per
# line, then the one-line description the downloads page renders.
#
# This file is the single place that answers "what do we publish". One script
# holds it true, three times: --wired proves the release workflow calls it at
# all; --staged compares this list against the files actually staged, before
# the release is created; --published <tag> compares it against the release
# that exists. Changing what the workflow publishes without changing this file
# fails the release before it is created.
*.debian13.deb             the agent and the prompter, built in a Debian 13 container
*.ubuntu2604.deb           the agent and the prompter, built in an Ubuntu 26.04 LTS container
*.fedora43.rpm             the agent and the prompter, built in a Fedora 43 container
*.fedora44.rpm             the agent and the prompter, built in a Fedora 44 container
*.opensusetw.rpm           the agent and the prompter, built in an openSUSE Tumbleweed container
librelinux_*.orig.tar.gz   the deterministic source tarball
sbom.cdx.json              CycloneDX bill of materials naming the statically linked agent core and QCBOR
SHA256SUMS                 checksums over every other asset in this release
*.sigstore.json            cosign keyless signature bundle, one per asset
